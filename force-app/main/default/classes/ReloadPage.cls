public with sharing class ReloadPage {
 	@InvocableMethod
    public static void refreshPage(List<Id> recordIds) {
        // This method does not need to perform any action.
        // It's used to trigger a screen flow, and the flow itself will handle refreshing.
    }
}