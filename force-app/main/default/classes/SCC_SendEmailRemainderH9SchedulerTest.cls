/**
 * @description       : Test class for SCC_SendEmailRemainderH5Scheduler
 * @author            : Generated by Claude
 * @group             : Test
 * @last modified on  : 03-03-2025
 * @last modified by  : Kenny
**/
@isTest
private class SCC_SendEmailRemainderH9SchedulerTest {
    
    /**
     * Test the execution of the scheduler
     * Just verify that it runs without errors
     */
    @isTest
    static void testSchedulerExecution() {
        Test.startTest();
        
        // Create and execute the scheduler directly
        SCC_SendEmailRemainderH9Scheduler scheduler = new SCC_SendEmailRemainderH9Scheduler();
        scheduler.execute(null);
        
        Test.stopTest();
        
        // No assertions needed - we're just verifying it runs without errors
    }
    
    /**
     * Test that the job can be scheduled via the Apex Scheduler
     */
    @isTest
    static void testScheduleJob() {
        Test.startTest();
        
        // Schedule the job
        String cronExp = '0 0 8 * * ?'; // Daily at 8 AM
        String jobId = System.schedule('Test Email Reminder Scheduler', 
                                       cronExp, 
                                       new SCC_SendEmailRemainderH9Scheduler());
        
        Test.stopTest();
        
        // Verify that the job was scheduled
        CronTrigger ct = [SELECT Id, CronExpression, TimesTriggered, NextFireTime 
                          FROM CronTrigger 
                          WHERE Id = :jobId];
        
        System.assertEquals(cronExp, ct.CronExpression);
        System.assertEquals(0, ct.TimesTriggered);
        System.assertNotEquals(null, ct.NextFireTime);
    }
}