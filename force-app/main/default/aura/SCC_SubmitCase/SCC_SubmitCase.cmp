<aura:component implements="lightning:availableForFlowScreens,force:appHostable,flexipage:availableForAllPageTypes,flexipage:availableForRecordHome,force:hasRecordId,forceCommunity:availableForAllPageTypes,force:lightningQuickAction" access="global">
    <!-- Attributes -->
    <aura:attribute name="flowApiName" type="String" default="ME_Case_Submit_Button"/>
    <aura:attribute name="recordId" type="String"/>
    <!-- <aura:attribute name="showConfirmDialog" type="Boolean" default="false"/> -->

    <!-- Lightning Services -->
    <!-- <lightning:navigation aura:id="navService"/>
    <lightning:flow aura:id="flowData"/> -->
    
    <!-- Modal Component -->
    <lightning:overlayLibrary aura:id="overlayLib"/>

    <!-- Confirmation Modal -->
    <!-- <aura:if isTrue="{!v.showConfirmDialog}">
        <div role="dialog" class="slds-modal slds-fade-in-open">
            <div class="slds-modal__container">
                <div class="slds-modal__header">
                    <h2 class="slds-text-heading_medium">Confirmation</h2>
                </div>
                <div class="slds-modal__content slds-p-around_medium">
                    <p>Pastikan tiket sudah Anda Save sebelum di Submit</p>
                </div>
                <div class="slds-modal__footer">
                    <lightning:button label="Tidak" onclick="{!c.handleCancel}" variant="neutral"/>
                    <lightning:button label="Ya" onclick="{!c.handleSubmit}" variant="brand"/>
                </div>
            </div>
        </div>
        <div class="slds-backdrop slds-backdrop_open"></div>
    </aura:if> -->
    
    <lightning:layout horizontalAlign="center">
        <lightning:layoutItem size="12" padding="around-small">
            <lightning:messages />
            <div class="slds-text-align_center">
                <lightning:button 
                    type="submit" 
                    name="Submit" 
                    label="Submit" 
                    class="slds-m-top_medium" 
                    onclick="{!c.handleSubmit}" 
                />
            </div>
        </lightning:layoutItem>
    </lightning:layout>
</aura:component>